type: transformer
handle: readme2sentiment
data:
  inputFilter: {
    "type": "object",
    "properties": {
      "type": {
        "const": "readme"
      }
    },
    "required": [
      "type"
    ]
  }